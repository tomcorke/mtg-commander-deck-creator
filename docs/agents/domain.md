# Domain Docs

This is a single-context repository.

Before exploring:

- Read `CONTEXT.md` if it exists.
- Read relevant ADRs in `docs/adr/` if they exist.
- Proceed silently when either location does not exist.

Use terminology from `CONTEXT.md` when it exists. Create domain documentation lazily when domain terms or decisions become stable.
